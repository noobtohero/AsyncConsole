# AsyncConsole: FreeRTOS Non-blocking Logger for ESP32

[![PlatformIO](https://img.shields.io/badge/PlatformIO-Compatible-orange.svg)](https://platformio.org/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

**AsyncConsole** คือ Library สำหรับการทำ Logging บน ESP32 ที่ทำงานภายใต้ FreeRTOS โดยใช้ระบบ Queue เพื่อป้องกันไม่ให้การส่งข้อมูลผ่าน Serial ไปหน่วงการทำงานของ Task หลักที่ต้องการความเร็วสูง

---

## 🚀 จุดเด่น (Highlights)

- **Non-blocking Performance**: แยกการทำงานของ Serial ออกเป็น Task อิสระ (Serial จะไม่ดึงงานหลักคุณให้ช้าลงอีกต่อไป)
- **Plug & Play**: ใช้งานง่ายเหมือน `Serial.printf()` แต่ฉลาดกว่า
- **Zero Overhead**: ตัดโค้ดทิ้งอัตโนมัติเมื่อไม่อยู่ในโหมด Debug (ช่วยประหยัด Flash/RAM)
- **Professional Look**: รองรับสี ANSI, Metadata (Timestamp/Task Name) และ Progress Bar
- **Safe & Reliable**: ระบบป้องกัน Queue เต็ม และฟังก์ชัน Flush เพื่อความมั่นใจก่อน Reboot

---

## 🛠 วิธีติดตั้ง (Installation)

### สำหรับ PlatformIO
ก๊อปปี้โฟลเดอร์ `lib/AsyncConsole` ไปไว้ในโปรเจกต์ของคุณ หรือเพิ่มใน `platformio.ini`:
```ini
lib_deps = 
    https://github.com/noobtohero/AsyncConsole.git
```

### สำหรับ Arduino IDE
ดาวน์โหลดไฟล์จาก GitHub แล้วแตกไฟล์ไว้ในโฟลเดอร์ `libraries` ของ Arduino

---

## ⚙️ การกำหนดค่า DEBUG_MODE (สำคัญมาก)

เพื่อให้ Library ทำงาน คุณ **ต้อง** นิยาม `DEBUG_MODE` ด้วยวิธีใดวิธีหนึ่งดังนี้:

1. **ในไฟล์โค้ด (`main.cpp`)**: ต้องวางไว้ **ก่อน** include library
   ```cpp
   #define DEBUG_MODE
   #include <AsyncConsole.h>
   ```

2. **ใน `platformio.ini` (แนะนำ)**:
   ```ini
   build_flags = 
       -D DEBUG_MODE
   ```

> [!TIP]
> เมื่อต้องการทำโปรเจกต์ลงเครื่องจริง (Production) เพียงแค่ลบการนิยามนี้ออก โค้ดที่เกี่ยวข้องกับ Logger ทั้งหมดจะถูกตัดทิ้งทันที ช่วยประหยัดพื้นที่และเพิ่มความเร็วได้ 100% ครับ

---

## 📝 ตัวอย่างการใช้งานอย่างรวดเร็ว (Quick Start)

```cpp
#define DEBUG_MODE
#include <AsyncConsole.h>

void setup() {
    CONSOLE_INIT(115200);
    CONSOLE_INFO("AsyncConsole is ready!");
    
    for(int i=0; i<=100; i+=20) {
        CONSOLE_PROGRESS("Booting", i);
        delay(100);
    }
}

void loop() {
    CONSOLE_DEBUG("Looping... millis: %lu", millis());
    delay(2000);
}
```

---

## 📚 ข้อมูลเพิ่มเติม
สามารถอ่านรายละเอียดคำสั่งและเทคนิคการใช้งานทั้งหมดได้ที่:
👉 **[AsyncConsole Documentation (README.md)](https://github.com/noobtohero/AsyncConsole/blob/master/lib/AsyncConsole/README.md)**

---

## 📄 License
MIT License - Copyright (c) 2024 NTH
